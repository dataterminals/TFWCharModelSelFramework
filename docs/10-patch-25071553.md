# Build 25071553 — the crash, the rebuild, and what is still unknown

> ## ⚠ SUPERSEDED IN PART — read [`11-container-staleness-refuted.md`](11-container-staleness-refuted.md) first
>
> **The mechanism this document proposes for the crash is WRONG.** Section "P0 — why it crashes"
> blames a changed global script-object store. Measured three independent ways on 2026-09-10, that
> is refuted: script imports are content-addressed hashes, not positions; the store change is
> additive (81 added, **2** removed, 0 changed ids); and all **220** of the shipped July pak's
> ScriptImports resolve against the live store with zero dangling. The v0.2.5 rebuild is a **null
> intervention** with respect to it — 193 of 199 packages come out byte-identical with identical
> import lists.
>
> Everything else here still stands and was independently re-verified: the field reports, the build
> facts, the frozen-ABI and reversion measurements, the two shut gates, and the P1 analysis. Only
> the causal claim is retracted. **CMSF's launch crash is currently unexplained.**

**STATUS 2026-09-09 (as written): the framework pak crashes the game at launch on `0.9.5.0`+.
Confirmed by three independent reporters. The pak has been REBUILT on the live cook and verified as
far as static analysis can go; it is UNVERIFIED IN-GAME and must not ship until someone has loaded
it.**

The short version for anyone picking this up cold. v0.2.4 ships a pak cooked **2026-07-21**; the
game is now four announced versions past that. The first suspect was *content* staleness — that our
whole-asset override deletes developer changes — and that suspect is dead: the pak reverts nothing,
pins nothing dead, and its copies of the overridden assets differ from live by 2–23 scalar bytes at
identical length.

**The pak does crash the game, but the reason proposed below is wrong.** ~~A zen container resolves
its script imports through the game's global script-object store, and that store changed:
3,012,295 bytes at the July cook, 3,017,553 today.~~ The store change is additive and provably
inert — see [`11-container-staleness-refuted.md`](11-container-staleness-refuted.md). The crash
cause is open, and the leading untested candidate is `PackageImport` **public-export-hash**
staleness, which is a different mechanism from the script store.

The lesson that does survive: **content equivalence is not survivability.** The seven owned assets
can be byte-for-byte fine and the container still fail to load — that framing was right, and only
the specific mechanism was wrong.

## Priority list, from the field

| | issue | evidence | status |
|---|---|---|---|
| **P0** | **Crash at launch with the pak installed**, from `0.9.5.0` | bidouboy84 2026-08-29; RyanLane 2026-09-06 (tells users to delete `CMSF_core_9`); Baron DM 2026-09-09 | pak rebuilt, **needs an in-game load** |
| **P1** | Skin menu absent for every character the user has no entitled skin for | TheLonerStar 2026-07-30; PinoIsReading 2026-07-24; Sigfried 2026-08-19 (**after** the v0.2.4 fix); Baron's second claim | **open — v0.2.4 did not fix it** |
| P2 | CMSF tiles flash a white icon, then vanish | srap2552 2026-07-31 | open — cosmetic, it is `prune()` |
| P3 | Adopt existing replace-style skin paks into CMSF slots | DravenRayne 2026-08-10, Zoeytrope +1 | feature request, promised "I'll see what I can do" |

P1 is the one to look at next, and PinoIsReading named the mechanism back in July without it being
picked up: *"if you didn't buy any skin dlc the option to select skins is not there... mask man is
the only that has that option because they gave a free skin with the game's first anniversary."*
So the selector panel's very existence appears gated on the character having at least one **entitled**
skin — a different gate from `SelectLockedSkinsOnly`, which is all `CMSFUnlock` currently clears.
`CMSFUnlock` cannot unfilter a panel the game never built. That would explain every P1 report
including Baron's, and it means the fix is a *new* gate to find, not a repair of the v0.2.4 one.

---

## The report

DM, 2026-09-09, from a player who installed CMSF to use a Scav Girl skin:

> It works, but only without the .pak/.ucas/.utoc files. However, it still doesn't unlock the menu
> for the characters that are supposed to have a selection option — specifically, the one I
> installed the mod for (Scav Girl).

Two claims, and they are separable:

1. **the pak trio is not survivable** — the configuration only works with `CMSF_Core_9_P.*` removed
2. **the selector does not unlock** — even in that pak-less configuration, Scav Girl's menu does
   not gain its base skins

Claim 2 in a pak-less install is `CMSFUnlock` alone, which is the half that does not need the pak.
Claim 1 and claim 2 have *different* causes and must not be chased as one bug.

**Both claims are corroborated on the Nexus posts tab**, which settles the ambiguity in the DM:

> **bidouboy84**, 2026-08-29 — "The mod dons't works since update 0.9.5.0... :/ the game crash at
> launch."
>
> **RyanLane**, 2026-09-06 — "guys if the game crash .Delete the File which called CMSF_core_9
> (The Forever Winter\Windows\ForeverWinter\Content\Paks\Mods)"
>
> **donjuan2999**, 2026-09-06 — "so you are telling us to delete the mod?"

So claim 1 is **crash on launch**, it is not specific to one player, and it dates to `0.9.5.0`
(2026-08-28) — not to any of the three later hotfixes. Users have been working around it by
deleting the pak for nearly two weeks, and the top comment on the mod page currently tells them to.
156 unique downloads are affected.

## The build

| | |
|---|---|
| installed build | `25071553`, paks written 2026-09-03 11:57 |
| shipping exe | 169,740,288 B / `D87AE674…` (was 169,641,472 / `5D9F12E6…`) |
| game versions folded in | `0.9.5.0` (08-28), `0.9.5.1` hotfix, `0.9.5.2` (08-31), `0.9.5.3` (09-02) |
| CMSF v0.2.4 was built on | the **2026-07-21** cook, i.e. build `24479102`-era |
| rollback key | `4492887131597018203` |

The landing, the filelist diff and the gate results are recorded in
`tfw-update-ops/state/build-history.md` under "24536482 -> 25071553".

Relevant patch-notes lines, because they set the priors:

- **"Updated Scav Girl shoulders"** and **"Updated Gunhead mesh"** — character meshes changed
- **Shaman: "improved materials"** — and the filelist confirms three MAY-skin material renames
- first-person hand/arm fixes for Bagman, Scav Girl and Shaman
- "Fixed the wrong character skin occasionally appearing when loading into a session"

That last one is the developers working in the same subsystem CMSF drives, which is worth knowing
but is not by itself a CMSF problem.

## What was measured, and what each measurement is worth

Ordered by how much weight it can carry. The first three read no type map and are the load-bearing
ones; see "Why nothing was rebuilt" for why that distinction matters this cycle.

### 🟩 The frozen ABI is intact — `grep -c "^ForeverWinter/Content/CMSF/"` returns **0**

The one check in this repo that guards *other people's* paks. The developers have not collided
with the slot namespace, so every claimed third-party slot in the wild is still addressable. This
is filelist-only, so it is as solid as it gets.

### 🟩 Nothing CMSF owns or reads has moved

Every path is present at its exact spelling in the 76,321-entry live filelist: `DT_SkinUIData`, all
six `BP_Player_*`, `ST_FW_UI_Skins`, `WBP_SkinSelection`, `WBP_SkinButton`,
`WBP_PlayerStatusWidget`, `WBP_MenuMaster`, `WBP_ReadyRoom`, `GA_Player_ChangeSkin`,
`DT_Entitlements`, `DT_EntitlementTags`.

This matters most for `CMSFUnlock`, whose documented failure modes are all *renames* that fail
silently — the claim signal returning nil, or `Init()` disappearing. At the asset level, none of
them fired. It does **not** clear the runtime members (`SelectLockedSkinsOnly`, `Init`,
`SkinOptions`, `SkinRow.RowName`, the `SkinIcon.Brush.ResourceObject` chain), which live inside
those assets and cannot be checked without running the game.

The filelist diff is 153 added / 142 removed and is mostly **directory-case churn**
(`Posed/`→`posed/`, `DataLayers/`→`Datalayers/`, `images/`→`Images/`, `TOOTHY/`→`Toothy/`,
`Bagman/`→`BagMan/`). Benign: UE lowercases the package name before hashing `FPackageId`.

### 🟩 The shipped pak's overridden assets differ from live by 2–23 scalar bytes

The strongest result here, and the one that killed the staleness theory. `retoc to-legacy` takes
**no usmap**, so this is a raw byte comparison of the game's own asset, July cook versus September
cook, and it is immune to the type-map problem below.

Full result, now automated as `verify_build.sh` check [5] — **6 of 14 files drifted**:

| asset | `.uasset` | `.uexp` |
|---|---|---|
| `BP_Player_BagMan` | identical | **3** bytes differ, same length |
| `BP_Player_Girl` | identical | **7** bytes differ, same length |
| `BP_Player_Gunhead` | identical | **23** bytes differ, same length |
| `BP_Player_MaskMan` | identical | **3** bytes differ, same length |
| `BP_Player_OldMan` | identical | **2** bytes differ, same length |
| `BP_Player_Shaman` | identical | **3** bytes differ, same length |
| `DT_SkinUIData` | identical | **identical** |

Every `.uasset` header is byte-identical, every `.uexp` is the **same length**, and
`DT_SkinUIData` did not change at all. That rules out the failure class everyone reaches for
first: the pawn Blueprints were not restructured, no export was added or removed, and the
July-cooked copies are not a structural mismatch for the September loader. What the stale pak
*does* revert is a handful of tuning scalars per pawn — real, worth fixing by rebuilding, and not
a crash. Gunhead's 23 is the largest and is consistent with the patch-notes line about its head
weapon aiming.

> **A trap worth recording.** Decoding the pak's `BP_Player_*` and diffing the JSON against the
> live base makes each shipped copy look ~25% *smaller*, which reads as "the pak deletes a quarter
> of the Blueprint". It is an artifact. `verify_build.sh` mounts an override pak **alone** on
> purpose, so five component classes (`BPC_Stamina_C`, `BP_AC_HealthSFX_C`,
> `BP_AC_PlayerAimingSFX_C`, `BP_AC_PlayerSprint_SFX_C`, `BP_AC_Rig_SFX_C`) cannot resolve and
> decode as bare `UObject` with empty properties. The script's own header calls this out as a
> documented gap. Byte-compare through `retoc`; do not JSON-diff across an isolated mount.

### 🟩 `verify_build.sh` is clean against the live build

Run in full, exit 0:

- **0 dangling soft references** across 791 references in 199 dumps, for both the framework pak and
  the octogirl author pak. Every mesh and portrait path the frozen overrides pin still resolves.
- **Group 1 reversion check: 0 rows, soft paths or roster entries dropped.** Live `DT_SkinUIData`
  has **33 rows**; the pak carries all 33 plus 192 CMSF rows. `SkinChoices` goes base→base+32 on
  every character. **The developers added no skin rows and no roster entries this cycle**, so the
  staleness inversion did not fire.

Live Scav Girl roster, for reference: `SkinChoices` = 5 (`SK_SCV_FL` … `SK_SCV_FL4`),
`LockedSkinChoices` = 3 (`SPT`/BlindFang, `DEC`/Dec2025, `DSQ`/April2026). So **8** available
unfiltered, where `docs/00-findings.md` recorded 7 — one entitlement skin was added at some point.
Worth updating the "vanilla unfiltered Scav Girl is 7" field check in
`tfw-update-ops/state/asset-dependencies.md`, which is now off by one.

> Note `DT_SkinUIData` carries a row `Skin.Girl.MAY` pointing at
> `Female/Skins/MAY/SK_SCV_FL_May`, and that mesh is in **neither** of Scav Girl's roster arrays.
> A row without a roster entry does not appear — that is the game's own unfinished content, not
> something CMSF causes or can fix, and it is a plausible source of player confusion about which
> Scav Girl skins "should" be selectable.

### 🟩 Pak mods in general are fine on this build

Not a CMSF measurement, but it bounds the problem. A weapon overhaul shipped to Nexus on 2026-09-06
and several inventory/stat paks were updated 09-05, all without breakage reports; another modder
confirms a pak mod working "two updates early". Whatever is happening to this player, the game is
not rejecting third-party paks wholesale, and `FWPakManifest.json` — which enumerates only the base
containers, with a signature over that list — is not gating `Content/Paks/Mods/`.

## P0 — the mechanism proposed here is REFUTED (kept as the record of a wrong turn)

> Superseded by [`11-container-staleness-refuted.md`](11-container-staleness-refuted.md), which
> carries the measurements. Kept verbatim below because the reasoning error — treating a size delta
> as evidence of breakage, then rescuing the hypothesis with an invented exposure asymmetry when
> counter-evidence arrived — is the instructive part. The 220-of-220 resolving ScriptImports killed
> it outright.

Every check here mounts the pak **alongside the live `global.utoc`/`global.ucas`**, because a mod
pak has no ScriptObjects chunk of its own and cannot be read without one. That is necessary, and it
is also precisely why the crash is invisible: the checks supply the very global store the shipped
pak disagrees with, then confirm the content is fine. It is.

The store itself moved:

| | bytes |
|---|---|
| `scriptobjects.bin` extracted at the **July** cook (what v0.2.4's pak was packed against) | 3,012,295 |
| `scriptobjects.bin` extracted at the **live** cook | 3,017,553 |

**+5,258 bytes of new script objects.** A zen container serialises its imports of native classes,
functions, properties and structs as references into that global store; `retoc to-zen` resolves
them at pack time. Change the store and a pre-existing container's script imports no longer denote
what they did. The six pawn Blueprints are dense with such imports, and `BP_Player_*` is loaded
while the player character is constructed — at launch.

The timing corroborates it. `0.9.5.0` (2026-08-28) is the "ground-up combat AI overhaul", a new AI
state machine plus a client-side shooting rework — the kind of change that adds native classes and
therefore script objects. Every crash report dates to `0.9.5.0` and none to the three subsequent
hotfixes, which were behaviour tuning.

**This is a container-level failure, not a content-level one**, and the distinction is the useful
output of this cycle:

- *content staleness* — our override deletes developer changes — is what `verify_build.sh` checks
  [2]/[3]/[4] test, and it was genuinely absent.
- *container staleness* — our pak's script imports no longer resolve against the current global
  store — was never tested by anything, and is what actually broke.

Check [5] added earlier this cycle narrows the gap: it compares the live cook against
`build/framework/src`, so a stale build tree now shows up. It is still not a direct test of import
resolution. **The honest position is that no static check in this repo can currently prove a pak
loads.** Only launching the game does that, which is why P0 stays open until someone does.

> A cheap standing check falls out of this, and it needs no game launch and no usmap: compare
> `build/framework/src/scriptobjects.bin` against a fresh extraction from the live cook. If those
> differ, every pak built from that tree is suspect regardless of how clean its content is. Worth
> folding into check [5].

## The rebuild — done and statically verified, but NOT a fix

> The three verifications below are sound and still stand: they establish that the rebuilt pak
> carries exactly the bytes intended. They do not establish that it fixes anything. With respect to
> script imports the rebuild is a **null intervention** — 193 of 199 packages byte-identical to the
> July pak, identical import lists throughout.

Rebuilt on the live `25071553` cook:

```
FW_AES_KEY=... RETOC=... USMAP=... python tools/cmsf_framework.py --slots 32
```

The generator's own decode-back pass reports `rows=32/32 roster=32/32 st=32/32` on all six
characters, 0.57 MB, 3.0 KB per slot.

**The usmap problem was worked around, not ignored.** Gate 1b is red, and
`cmsf_framework.py` serialises through UAssetAPI against that map, so the build needed an
independent check that nothing was mis-serialised. Three, none of which trust the type map:

1. **The append is exactly the right size.** Every pawn `.uexp` came out at
   `live_src + 672 bytes` — the 32 CMSF `SkinChoices` entries, and nothing else. A stale schema
   would almost certainly have changed the length.
2. **The pak round-trips its payload byte-for-byte.** Converting the built pak back with
   `retoc to-legacy` (no usmap) and comparing against what was staged: **199 of 199 `.uexp`
   payloads identical.** The `.uexp` is the serialised property data, so every byte the pak
   carries is exactly what was intended. The `.uasset` headers all differ — first difference at
   byte 29, sizes changed — because retoc rebuilds the legacy package summary from zen structures.
   That is a property of the conversion, not a defect; the payload is what matters.
3. **`verify_build.sh` is fully green**, check [5] included: *"14 file(s) byte-identical to the
   live cook — the pak was built on THIS cook."*

Positive evidence that the skin-side type layout did not move, which is why the stale map was
survivable here: `DT_SkinUIData` is **byte-identical** July↔September, and the six pawn `.uexp`
files are unchanged in **length**. Unversioned serialisation is schema-order dependent, so a
changed layout would move offsets. The demonstrated staleness is in `FWWeaponDefinition`, a weapon
struct CMSF never touches.

**What this does NOT establish.** That it loads. The mechanism above is a *container* failure, and
none of the three checks tests import resolution — they test that the pak carries the bytes we
meant. Per the house rule this is **unverified in-game**, and the release bundle is deliberately
**not built**: a stale `dist/release/` zip is recoverable, a published broken one is not.

## The two gates that are still shut, and both need a launch

### 🟥 Gate 1b — the usmap is stale, and it fails silently

Measured on the `FWWeaponDefinition` export of `DA_WPN_PLAYER_HRF01`, same extraction three ways:

| decode | properties |
|---|---|
| build `24536482` with its own correct map | **57** |
| the baseline's kept `-STALE-USMAP` sample | 30 |
| **build `25071553` with the current map** | **30** |

57 is the exact number `provenance.json` records as this map's verification, and gate 1b's note in
`status.md` says it "was stopping at 30" before the last regeneration. It is stopping at 30 again,
with no error and plausible-looking values. `bConvergeADSAimToCamera` reads at index 28 of 30
instead of index 46 of 57 — the "real bytes under neighbouring property names" symptom, not a sign
of health.

This does not touch any 🟩 above. It does block the fix.

### ⬜ Gate 3 — `CMSFUnlock` has never run on this exe

There is no `UE4SS.log` anywhere under `D:\MO2_InstanceData\TheForeverWinter`. The game has not
been launched under MO2 since 2026-08-04. So:

- whether UE4SS `-894` still attaches to a September binary is **unattempted, not merely unscored**
- whether `SelectLockedSkinsOnly`, `Init()` and the claim-signal chain still resolve is unknown
- the v0.2.4 forced-repopulate path has never been exercised against this build

Claim 2 in the field report lives entirely behind this gate. Note also that Root Builder's cache
is keyed on the engine version (`5_4_2_0`), unchanged since July, and has had no chance to rebuild
against `25071553` — so **the revert mechanism is armed for the next launch.** Clear
`GameData.json` and its sibling `Backup\` together *before* launching, or the measurement session
will be taken on a partially-July install.

## Order of operations from here

1. **Get `UE4SS.log` from a reporter** (asked for 2026-09-09) and settle gate 3.
2. **Load the rebuilt pak.** Clear Root Builder's `GameData.json` **and** its sibling `Backup\`
   together first — the game has not launched under MO2 since 2026-08-04, so the revert mechanism
   is armed and a session started without clearing it measures a part-July install.
3. If it launches, **P0 is fixed** and the script-object mechanism above is confirmed. If it still
   crashes, the mechanism is wrong and the next suspect is the pak's own container version against
   the new loader.
4. **Regenerate the usmap** (experimental UE4SS → `DumpUSMAP()`, then remove UE4SS). Archive the
   outgoing `24536482` map and update `provenance.json` in the same commit. Then re-run
   `verify_build.sh` and, ideally, rebuild once more on a trustworthy map before shipping.
5. **Then attack P1**, which is the older and more-reported bug. Find the gate that decides whether
   the selector panel exists at all for a character with no entitled skin; `SelectLockedSkinsOnly`
   is not it.
6. Only then package and ship. `cmsfunlock`'s `selector lists N skin(s)` at Scav Girl is the
   fastest field check: **8** = unfiltered vanilla, **40** = framework loaded and populating,
   **0** = the panel read failed.

`FW_AES_KEY` is required for a rebuild and is not in this repo by design. The key is embedded in
`forever-winter-datamine/datamine/decoder/Program.cs` and is noted there as constant across
patches; supply it via the environment and do not commit it here. `RETOC` and `USMAP` also need
setting — `fwlocate` looks for retoc in `tools/retoc/` or on PATH, and does not know about the copy
inside `dist/release/CMSF-author-*/`.

## What to ask the reporter for

Claim 1 is now explained and a fix is built, so the remaining questions are all about **claim 2**
(P1), which is the open bug. Asked for on 2026-09-09. In priority order:

1. **`UE4SS.log`.** Settles gate 3 for their machine in one file — whether UE4SS attached, whether
   `CMSFUnlock` loaded, and which version banner it printed (`v0.2.4 loaded — …`). This is the one
   that matters now.
2. **Which characters have a skin menu at all, and whether they own any skin DLC.** This is the
   direct test of PinoIsReading's mechanism: if the menu exists only for Mask Man — whose
   anniversary skin everyone got free — then the panel is entitlement-gated and P1 is not a
   `CMSFUnlock` filtering bug at all. ~~What "breaks" means with the pak installed~~ — answered by
   the Nexus reports: crash on launch.
3. **The exact install layout**, verbatim paths. `CMSF_Core_9_P.{pak,ucas,utoc}` belongs in
   `Windows\ForeverWinter\Content\Paks\Mods\`, and `CMSFUnlock` under
   `Windows\ForeverWinter\Binaries\Win64\ue4ss\Mods\CMSFUnlock\` with its `enabled.txt` — **not**
   as a pak mod. That specific mistake has already been the answer once in the TFW Discord.
4. **Which Scav Girl skin mod**, and whether it is a CMSF slot pak or a conventional
   package-identity overwrite. An overwrite skin and CMSF are not the same product and do not need
   each other; if theirs is an overwrite mod, the framework pak is not what makes it appear and
   removing it is correct.
5. **UE4SS build.** `CMSFUnlock` is developed against experimental `v3.0.1-894-g2172883`. A stable
   release may load the Lua and still not expose what the mod reads.
6. Whether any other pak mod is installed that touches player characters.

## Loose ends this cycle turned up

- ~~**`verify_build.sh` cannot see whole-asset drift.**~~ **Fixed this cycle.** It compared only
  rows, soft paths and roster entries, so the scalar reversion above passed as "clean". Check
  **[5] cook drift** now byte-compares each overridden asset — live cook versus
  `build/framework/src`, the pristine extraction the pak was built from — through
  `retoc to-legacy`, which is passed **no usmap** and is therefore the one check here that still
  works when gate 1b is red. It is opportunistic: missing retoc, `FW_AES_KEY` or the local build
  tree is a SKIP that is carried into the RESULT line, never a silent pass. Drift is reported and
  surfaced in RESULT but does **not** set the exit code — a drifted cook means a rebuild is owed,
  not that the pak is broken.
- **`docs/00-findings.md` and `asset-dependencies.md` say "unfiltered Scav Girl is 7".** It is 8
  on this build. Small, but it is the documented field check.
- **The Shaman MAY material renames** are direct exposure for `forever-winter-skin-mods`, not for
  CMSF. Routed to that repo's owner via the ops board rather than handled here.
- **Check [5] should also compare `scriptobjects.bin`.** The P0 mechanism was a changed global
  script-object store, and comparing the build tree's copy against a fresh extraction is a
  two-line, usmap-free, launch-free test for "this build tree is too old to pack from". It would
  have flagged v0.2.4 as unsafe on 2026-08-28 instead of on 2026-09-09.
- **The mod page needs a pinned note.** The current top comment is a user telling people to delete
  the pak, and it is correct advice for v0.2.4. Anyone landing on the page today has no way to know
  a fix is coming.
- **P3 is more attractive than it looks.** Two users asked to bring existing replace-style skin
  paks into CMSF slots. Those mods rewrite package identity to collide with a shipped skin; a CMSF
  slot wants the mesh cloned to a frozen path instead. `tools/cmsf_author.py` already does exactly
  that transformation, so the gap is a converter front-end, not new mechanism.
